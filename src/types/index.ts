export interface Trip {
    title: string;
    from: string
    destination: string;
    photos: string[];
    startDate: string;
    endDate:string;
    slug: string;
    details: string;
}

export type TDestination ={

}